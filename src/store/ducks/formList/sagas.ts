import { call, put } from 'redux-saga/effects';

import api from '../../../services/api';

// import {  loadIdCard } from './actions';

export function* load(): any {
  try {
    console.log('test');
  } catch (err) {
    console.log('test');
  }
}
