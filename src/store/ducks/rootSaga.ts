import { all, takeLatest } from 'redux-saga/effects';

// import { VerificationTypes } from './verificationDocs/types';
// import { load } from './verificationDocs/sagas';

// export default function* rootSaga(): any {
//   return yield all([takeLatest(VerificationTypes.LOAD_IDCARD, load)]);
// }
